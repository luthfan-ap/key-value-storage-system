syntax = "proto3";

message ValueData {
    string data = 1; // Original field
    int32 number = 2;
    // Adding new fields later, e.g., int32 timestamp = 2;
}

message KeyValue {
    string key = 1;
    ValueData value = 2;
}