syntax = "proto3";

message ValueData {
    string data = 1; // Original field
    // Adding new fields later, e.g., int32 timestamp = 2;
}

message KeyValue {
    string key = 1;
    ValueData value = 2;
}